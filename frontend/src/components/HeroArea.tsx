import * as React from "react";
import styled from "styled-components";

export const HeroArea = () => <HeroAreaStyle>hero-area</HeroAreaStyle>;

const HeroAreaStyle = styled.div`
  width: 100%;
  color: white;
  background-color: #f26964;
  height: 500px;
`;
